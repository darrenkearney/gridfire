var game = new Phaser.Game(64, 64, Phaser.AUTO, '', { preload: preload, create: create, update: update });

function preload() {

    // game.load.image('sky', 'assets/sky.png');
    // game.load.image('ground', 'assets/platform.png');
    // game.load.image('star', 'assets/star.png');
    // game.load.spritesheet('dude', 'assets/dude.png', 32, 48);

}

function create() {

    // grid = []
    // for y in range(3):
    //     grid_x = []
    //     for x in range(3):
    //         cell = Cell(x=x, y=y)
    //         grid_x.append(cell)
    //     grid.append( grid_x )

}

function update() {
}